name: Deploy to GitHub Pages`n`non:`n  push:`n    branches: 'main'`n`npermissions:`n  contents: read`n  pages: write`n  id-token: write`n`nconcurrency:`n  group: 'pages'`n  cancel-in-progress: true`n`njobs:`n  deploy:`n    environment:`n      name: github-pages`n      url: `${{ steps.deployment.outputs.page_url }}`n    runs-on: ubuntu-latest`n    steps:`n      - name: Checkout`n        uses: actions/checkout@v4`n`n      - name: Setup Bun`n        uses: oven-sh/setup-bun@v1`n        with:`n          bun-version: latest`n`n      - name: Install dependencies`n        run: bun install`n`n      - name: Build`n        run: bun run build`n`n      - name: Setup Pages`n        uses: actions/configure-pages@v4`n`n      - name: Upload artifact`n        uses: actions/upload-pages-artifact@v3`n        with:`n          path: './dist'`n`n      - name: Deploy to GitHub Pages`n        id: deployment`n        uses: actions/deploy-pages@v4
